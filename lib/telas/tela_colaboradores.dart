import 'package:flutter/material.dart';

class TelaColaboradores extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gerenciamento de Colaboradores'),
      ),
      body: Container(),
    );
  }
}
